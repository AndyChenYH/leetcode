#include <stdio.h>
#include <math.h>

int LM, LN;
long long fac(
int main() {
	freopen("inp.in", "r", stdin);
	scanf("%d %d", &LM, &LN);
	int p = 1;
	for (int c = 1; c <= LM + LN; c ++) {
		p *= c;
	}
	p /= 
	return 0;
}
